export declare abstract class Layout {

    /**
     * construct HTML element maybe with options to use it in construct components
     * @return{HTMLElement}
     * @param args
     */
    public abstract construct(...args): HTMLElement;
}
